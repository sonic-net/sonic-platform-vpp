SPATH       := $($(VPPINFRA)_SRC_PATH)
DEP_FILES   := $(SONIC_COMMON_FILES_LIST) platform/vpp/rules/vpp.mk platform/vpp/rules/vpp.dep
DEP_FILES   += $(SONIC_COMMON_BASE_FILES_LIST)
SMDEP_FILES := $(addprefix $(SPATH)/,$(shell cd $(SPATH) && git ls-files))

$(VPPINFRA)_CACHE_MODE  := GIT_CONTENT_SHA
$(VPPINFRA)_DEP_FLAGS   := $(SONIC_COMMON_FLAGS_LIST)
$(VPPINFRA)_DEP_FILES   := $(DEP_FILES)
